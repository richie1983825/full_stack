# 后端 API 基础路径
VITE_API_BASE_URL=/api

# 是否启用 Mock 后端（true=MSW 模拟，false=代理到真实后端）
VITE_ENABLE_MOCK=false

# 真实后端地址（仅 VITE_ENABLE_MOCK=false 时生效）
VITE_API_PROXY_TARGET=http://localhost:3101

# 用户可见的对外地址（快照分享链接），开发环境通常为前端端口
VITE_PUBLIC_BASE_URL=http://localhost:3100
